# Bonto and other yarn v1 hosts resolve devDependencies even with --production,
# and abort the whole install on an engine mismatch. The dev tooling is not
# what they are installing, so do not let it block express and socket.io.
ignore-engines true
